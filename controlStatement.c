#include<stdio.h>
int main() {
  //Local variable defination..
  int a = 10;

  //checking boolean conditions using if statement..

  if (a < 20) {
    //if boolean statement is true,, than the output will be printed
    printf("a is less than 20");
  } else {
    //if flase...
    printf("a is not less than 20");
  }

  return 0;
}
