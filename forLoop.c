#include <stdio.h> 
int main() {
  printf("Numbers between 1 to 10:\n ");
  for (int i = 1; i <= 10; i++) { // i can be instances, starting from 1 and will continue til 10 with the operator <=, and incremental operator ++
    printf("%d\.n",i); // it will print the numbers as i;
  }

  return 0;
}
